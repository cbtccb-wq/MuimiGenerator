# MuimiGenerator — Python dependencies
# Update this file when adding packages.
# Pin versions for reproducibility.

# Core dependencies (add as needed)

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Linting / type checking
ruff>=0.4.0
mypy>=1.10.0
